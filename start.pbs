#!/bin/bash
#PBS -N parco-job
#PBS -o ./parco-job.out
#PBS -e ./parco-job.err
#PBS -q short_cpuQ
#PBS -l walltime=0:10:00
#PBS -l select=1:ncpus=64:mem=1gb

module load gcc91
module load mpicc-3.2.1--gcc-9.1.0
export OMP_NUM_THREADS=64
cd PARCO
make all

if [ -z "$flags" ]; then
  echo "Flags are not set, running full benchmark"
  ./bin/out --benchmark-full
else
  ./bin/out $flags
fi
